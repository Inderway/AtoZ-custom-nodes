## image
###  Downscale Image (threshold)
等比例缩小大于阈值的图片，边缘使用模糊padding。

### Reset Resolution by Longer Edge
根据输入图片的宽高，重置给定的宽高分配。例：输入宽高为800和600，输入图片的实际宽高为60和80，由于图片的高大于宽，所以输出宽高应该为600和800；若图片的宽高为90和70，则输出图片宽高为800和600。
输入：width(int)，height(int)，image
输出：output_width，output_height

### Load Image (index)
使用索引读取指定文件夹的图片，文件夹中图片的排序要与windows系统的按名称排序对应，而不是字典序。比如： 2.jpg应当排在10.jpg前面。
输入：索引(int)，文件夹路径(string)
输出：image

## text
### prompt selector
使用索引读取指定文件夹的txt文件，并输出文本。
输入：索引(int)，文件夹路径(string)
输出：文本(string)

## file
### file selector
使用索引读取指定文件夹的指定格式的文件路径，并输出文件路径。比如文件夹中有2个mp4文件和5个其他文件，若输入限定了只读取mp4文件，则索引取值只可以为0和1。
输入：索引(int)，文件夹路径(string)
输出：路径(string)

